namespace EmployAI.Models  // for grouping related code inside the models folder (to keep things organized)
{
    public class Login  // Declares the Login class to hold login data
    {
        public string Username { get; set; }  // Property for storing the username
        public string Password { get; set; }  // Property for storing the password
    }
}
